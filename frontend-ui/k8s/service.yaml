apiVersion: v1
kind: Service
metadata:
  name: frontend-ui
  namespace: frontend
  labels:
    app: frontend-ui
    tier: web
    app.kubernetes.io/name: frontend-ui
spec:
  type: ClusterIP
  selector:
    app: frontend-ui
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
